{
    "example_group": [
        {
            "endpoint": "/users/{id}",
            "backend": "/api/v1/employee/{id}"
        },
        {
            "endpoint": "/posts/{id}",
            "backend": "/posts?postId={id}"
        }
    ]
}
